# SigLIP2 Single-View Merged Baseline
# Single-view SigLIP2: Qwen-Text merges the 3 sentences into 1 -> cosine similarity -> threshold

prompt:
    version: siglip2_single_view_merged
    extract_file: null
    verify_file: null
    nav_file: null
    category_file: "category_v1.yaml"
    merge_file: "merge_v1.yaml"

method:
    name: "clip"
    agent_type: "siglip2_single_view_merged"
    bbox_mode: "dino"
    clip_model: "./models/siglip2-so400m-patch14-384"
    clip_threshold: 0.5
    aggregation: "max"
    desc_aggregation: "merged"

env:
    max_steps: 1
    sectors: 6
    view_priority: "far"

dataset:
    root: "./data/pv_dataset"
    capture_subdir: "pin_capture"
    image_gt_root: "./data/pv_dataset/image_gt"
    category_cache_path: "./data/pv_dataset/category_cache.json"
    split: "val"
    index_file: "pv_index_50.jsonl"
    desc_db: "./data/pv_dataset/object_descriptions_with_category.json"

output:
    root: "./outputs/siglip2_single_view_merged"
    save_viz: true

server:
    qwen_text_url: "http://127.0.0.1:12182/qwen-text"
    qwen_vl_url: "http://127.0.0.1:12182/qwen-vl"
    gdino_url: "http://127.0.0.1:12183/groundingdino"

evaluation:
    unsure_as_negative: true
